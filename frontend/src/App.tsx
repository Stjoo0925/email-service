import React from "react";
import { BrowserRouter as Router, Routes, Route } from "react-router-dom";

function App() {
  return (
    <Router>
      <Routes>
        {/* 라우트 예시: <Route path="/" element={<div>홈</div>} /> */}
      </Routes>
    </Router>
  );
}

export default App;
